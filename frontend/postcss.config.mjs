// Tailwind CSS v4 is configured as a PostCSS plugin (no tailwind.config.js needed;
// the theme lives in app/globals.css via @theme).
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
